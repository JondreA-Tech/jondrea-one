import "./nido.css";
import "../../../../styles/product-shell.css";

/** Estilos de identidad Nido para la ficha de producto. */
export default function NidoProductLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
